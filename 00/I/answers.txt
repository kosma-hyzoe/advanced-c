Linijka 38: 

W praktyce zachowanie undefined - nie przypisaliśmy odpowiednio pamięci do
wskaźnika `buf`. ani pola `data.buf` (zakładając od-komentowanie linijki 42). Po
zrobieniu tego, zakładając odpowiednią długość stringa, ustawiłby pierwsze 8
wskazanych przez ptr bajtów (w tym wypadku od 8 do 16 bajta pola`buf` po
operacji `ptr += 8;`) na 0.

Linijka 45:

Segentation fault! Próbujemy przeprowadzić 'dereferencję' wskaźnika NULL. 
Zmieniając kod na `printf("%p\n", buf);` wyprintowalibyśmy ładnie '(nill)`.
